# TODOS

未来工作清单。

## 待办

### 打印审计日志查询页（来源：D8 in-scope，但仅做写入）

**What**: 在系统设置或操作日志页里加一个"打印审计"子页，按时间 / 单据号 / 操作员筛选 `print_log` 表。

**Why**: D8 在 v1 已经把"写入"做了，每次打印会落库；但缺一个查询界面让审计人员能看。

**Pros**:
- 越南合规审计可直接在系统里查"哪张单被谁打了几次"
- 数据已经在落库，前端工作量小（半天）

**Cons**:
- v1 暂无审计人员需求时是 YAGNI

**Context**:
- v1 仅做了 `log_print_event` 写入 IPC；缺一个 `list_print_logs(filter)` 读 IPC + 页面
- 数据库：`print_log` 表 + 索引已建好

**Depends on / blocked by**: v1 上线累积一段时间数据后再做。

---

### BOM 管理后续增强功能（来源：BOM 迭代实施计划 P2）

**What**: 在 P0/P1 迭代基础上，为 BOM 管理模块扩充以下后续增强功能：
1. **开料单子系统**：在 BOM 明细基础上，为海绵/木方/包装工序物料补充详细开料参数（长×宽×高mm、密度、部位说明等），以细化生产工艺路线。
2. **BOM 产品头信息扩展**：将产品尺寸、包装尺寸、净重/毛重、包装方式、每柜件数等从 Remark 抽离或与产品/物料主数据扩展字段联动；落地后需求计算的 TC 模式可自动带入装柜量。

**Done**（2026-07-13）:
- ~~多 SKU 并排比较视图~~：BOM 列表勾选 ≥2 个 BOM 进入用量对比视图，按工序分组、物料合并行，展示各 SKU 用量与合计。
- ~~每柜用量 (TC) 计算~~：需求计算支持「按装柜件数 (TC)」模式切换；装柜量自动填充依赖产品头扩展落地。

**Why**: 这些功能不阻塞真实的 BOM 数据录入和目前的数据流通，作为精细化生产和报表看板的后续增强，可以降低前期的改造风险，降低主业务逻辑负担。

**Depends on / blocked by**: 剩余两项均涉及数据库结构调整（新表 / 主数据扩展字段），需确认方案后实施。

